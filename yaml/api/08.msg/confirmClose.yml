tags:
  - 消息弹窗
  - 组件扩展
name: confirmClose
filetype: method
note: |
  关闭confirm弹窗信息<br>
  主要用于Android中的后退键关闭弹窗
warn: |
  只在Vue组件模式 并且 显示时传入的 options.<b>nativeFirst</b>为<b>false</b>时（默认值）可用
code: |   
  // 关闭弹窗
  $api.confirmClose()
changes:
  v1.0.1:
    - '[change] 由alert.hide 改为 alertClose'