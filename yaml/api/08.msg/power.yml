tags:
  - 消息弹窗
  - 组件扩展
name: power
filetype: method
note: |
  权限层
tip: |
  Vue组件模式下可用，只是项目中扩展的
args:
  necessary:
    type: Boolean
    note: 是否必须拥有权限，默认值：true，表示关闭会将来源页面或窗体一起关闭
    required: false
code: |        
  // 简单示例 
  $api.power()
  $api.power(false)
changes:
  v1.1.2:
    - '[feature] 新增：调用Vue组件，实现！'