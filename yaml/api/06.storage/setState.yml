tags:
  - Storage
  - 通用
name: setState
filetype: Method
note: |
  修改或添加键值(key-value)对数据到状态的值<br>
  相当于 setStorageObject 指定key的简写
args:
  key:
    type: String
    required: true
    note: 键名
  value:
    type: Any
    required: true
    note: 键值
code: |       
  // $state uid
  $api.setState('uid','mike')
changes: