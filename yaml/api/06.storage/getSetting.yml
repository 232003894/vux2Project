tags:
  - Storage
  - 通用
name: getSetting
filetype: Method
note: |
  通过键(key)检索获取设置的值
  相当于 getStorageObject 指定key的简写
args:
  key:
    type: String
    required: true
    note: 键名
return: 
  type: Any
  note: 对应键值
code: |       
  // $settings version
  $api.getSetting('version')
changes: